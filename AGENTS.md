# Codex 协作规则

这份文件用于约束 Codex 在本项目中的开发行为。

## 1. 产品方向优先级

本项目的核心是：

> 学习路径 + 多模态理解 + 实操闭环

任何新增功能都必须服务这个核心。

如果用户提出的需求会让产品偏向资讯门户、资源导航、社区或复杂后台，Codex 应该明确提醒，并给出更贴近学习闭环的替代实现。

## 2. 开发节奏

- 不要一次性做完整平台
- 每次只完成当前要求的模块
- 先理解当前任务在整个产品中的位置，再实现
- 优先做能跑通学习流程的功能
- 不为了“看起来很多”而堆模块

## 3. MVP 技术约定

优先使用：

- Vite
- React
- TypeScript
- 集中 mock data
- 清晰组件拆分

避免在 MVP 阶段引入：

- 复杂后端
- 复杂状态管理
- 自动抓取系统
- 用户系统
- 评论社区
- 支付系统

## 4. 代码组织原则

- 页面放在 `src/pages`
- 可复用组件放在 `src/components`
- 假数据放在 `src/data`
- 类型定义放在 `src/types`
- 样式保持统一，不要每个页面自成一套
- 模块命名要清楚，优先表达产品含义

## 5. 内容结构原则

每个学习主题都必须包含：

- 一句话定义
- 简明讲解
- 深入讲解
- 图示 / 结构化理解
- 视频资源
- 音频资源
- 今日实操任务
- 学习回顾 / 复盘问题

不能只有链接列表，也不能只有文章正文。

## 6. UI 原则

- 首页不是营销 landing page，而是学习入口
- 首屏重点是“今天学什么”
- 页面保持克制、现代、清晰
- 不做新闻流
- 不做社区 Feed
- 不做复杂管理后台
- 桌面优先，兼顾移动端
- 组件风格统一

## 7. 实操任务原则

每个实操任务都要有：

- 明确目标
- 背景说明
- 可直接给 Codex 的 prompt
- 预期产出
- 检查清单

任务应该尽量能在本项目内完成，帮助用户把概念变成可运行或可检查的东西。

## 8. 每次交付前检查

Codex 在完成开发任务后，应尽量检查：

- 页面是否能运行
- 样式是否统一
- 移动端是否基本可用
- mock data 是否集中管理
- 是否仍然服务“学习路径 + 多模态理解 + 实操闭环”

